# Example .env file for dev
# Copy this file to .env and fill in real values.
# NEVER commit a .env file with real credentials.

# ── Frappe / ERPNext ──────────────────────────────────────────────────────
# Standard dev URL for this repo:
#   http://localhost:8000/desk

# Publish port for the HTTP entrypoint (backend service). Avoid 80 on Windows.
HTTP_PUBLISH_PORT=8000

# Optional: force site resolution if you use a custom hostname
# FRAPPE_SITE_NAME_HEADER=localhost

# Site name (compose uses SITE_NAME; run.sh also accepts FRAPPE_SITE)
# Use `localhost` to avoid Windows hosts-file edits.
SITE_NAME=localhost
FRAPPE_SITE=localhost

# Database credentials (dev only)
DB_ROOT_PASSWORD=root
DB_NAME=frappe
DB_USER=frappe
DB_PASSWORD=frappe

# Frappe admin password (dev only)
ADMIN_PASSWORD=admin

# Apps to install at site creation time
INSTALL_APPS=erpnext,agriculture,yam_agri_core,yam_agri_qms_trace

# ── Yemen-specific: offline image archive ────────────────────────────────
# Path to the pre-pulled Docker image archive created by: bash run.sh prefetch
# Used by: bash run.sh offline-init
# Leave empty if always running with internet access.
OFFLINE_IMAGES_ARCHIVE=./offline-images.tar

# ── Yemen-specific: Docker resource limits ───────────────────────────────
# Keep total memory budget below available RAM (8 GB machine → 6 GB limit).
# Set these if your machine is low on memory:
#   DOCKER_MEMORY_LIMIT_DB=1g
#   DOCKER_MEMORY_LIMIT_FRAPPE=2g
# (Uncomment and add to individual service `mem_limit:` fields if needed)

# ── Yemen-specific: VPN / remote access ─────────────────────────────────
# If accessing the dev environment via WireGuard VPN from a remote site,
# set the Frappe site name to match the VPN-accessible hostname.
# Example: FRAPPE_SITE=yam-dev.vpn.internal

