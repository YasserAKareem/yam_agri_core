## Copy this file to .env and fill in secrets locally. Do NOT commit real credentials.
# Canonical environment variables used by infra/run.sh and docker-compose files
SITE_NAME=site1.local
DB_ROOT_PASSWORD=change_me
DB_NAME=frappe
DB_USER=frappe
DB_PASSWORD=change_me
ADMIN_PASSWORD=admin
FRAPPE_SERVICE=backend

# Optional offline archive path
OFFLINE_IMAGES_ARCHIVE=./offline-images.tar
